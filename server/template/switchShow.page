<template>
  <div class="swiper-box">
    <div class="swiper-container" id="auto-swiper">
      <div class="swiper-wrapper">
        <div class="swiper-slide card" @for="value in swiperList">
          <div class="swiper-item" >
            <a href="#">
              <img src="{{value.image}}"/>
              <div class="image-title">{{value.title}}</div>
            </a>
          </div>
        </div>
      </div>
    </div>
    <img src="{{--rightIcon--}}" class="swiper-button swiper-button-next" @click="swiperNext"/>
    <img src="{{--leftIcon--}}" class="swiper-button swiper-button-prev" @click="swiperPrev"/>
  </div>
</template>

<script>
  module.exports = {
    created: function () {
      this.data.swiper = new Swiper('#auto-swiper', {{--swiperSetting--}})
    },
    data: {
      swiper: null,
      swiperList: {{--swiperList--}}
    },
    swiperNext: function () {
      // console.log('swiperNext')
      this.data.swiper.swipeNext()
    },
    swiperPrev: function () {
      // console.log('swiperPrev')
      this.data.swiper.swipePrev()
    }
  }
</script>

<style lang="less">
  .swiper-box {
    background-color: {{--bgColor--}};
    .swiper-container {
      margin: 15px 40px;
      height: 200px;
      .swiper-item {
        width: 240px;
        height: 170px;
        position: absolute;
        left: 0;
        right: 0;
        top: 0;
        bottom: 0;
        margin: auto;
      }
    }
  }

  .swiper-box {
    position: relative;
    .swiper-button {
      width: 30px;
      height: 30px;
      position: absolute;
      top: 0;
      bottom: 30px;
      margin: auto;
      cursor: pointer;
    }
    .swiper-button-next {
      right: 10px;
    }
    .swiper-button-prev {
      left: 10px;
    }
  }
  a {
    color: black;
    display: block;
    width: 100%;
    height: 100%;
    text-decoration: none;
    img {
      width: 200px;
      height: 140px;
      margin: 0 20px;
    }
  }
  .image-title {
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    text-align: center;
    line-height: 25px;
  }
  .swiper-slide {
    position: relative;
  }
</style>